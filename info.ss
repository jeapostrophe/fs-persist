(module info (lib "infotab.ss" "setup")
  (define name "fs-persist")
  (define homepage "http://jay.makeoutcity.com/")
  (define blurb
    (list "fs-persist"))
  (define primary-file "fs-persist.ss")
  (define doc.txt "doc.txt")
  (define categories `(io)))